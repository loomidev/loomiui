---
"@loomidev/select": minor
---

The options panel now opens in the top layer, positioned against the trigger, so opening a select inside a modal (or any `overflow` container) no longer makes that container scroll. It flips above the field when there isn't room below and follows the field when an ancestor scrolls.
