---
"@loomidev/button-group": minor
---

Add a `circle` attribute (on the group or on a single item) for full-radius icon buttons, intended only for icon-only buttons. Also fixes `icon-only` padding/radius overrides being silently beaten by the group's inline size/radius style vars.
