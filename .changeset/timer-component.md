---
"@loomidev/timer": minor
"@loomidev/components": minor
---

Add `<loomi-timer>`, an animated count up/down timer with optional controls, count-up stopwatch mode, count-down mode, themed progress styling, lifecycle events, and host font-size inheritance through normal `style` and `class` attributes.
