---
"@loomidev/profile-menu": minor
---

Add an `avatar-position` attribute to `<loomi-profile-menu>`. Set it to `right` to put
the avatar after the name and description, with the chevron still trailing.
