---
"@loomidev/core": minor
---

Add `anchorFloatingPanel(anchor, panel, placement)`, which opens a dropdown panel in the top layer, places it against its anchor, and keeps it attached on scroll and resize. Components use it so their panels never make a modal or other `overflow` container scroll.
