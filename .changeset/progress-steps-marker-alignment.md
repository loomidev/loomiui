---
"@loomidev/progress": patch
---

Horizontal circle steps now top-align each marker with its label, so a description that wraps onto an extra line no longer drops that step's marker below the rest of the row.
