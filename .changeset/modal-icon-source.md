---
"@loomidev/modal": minor
---

Add an `icon-source` attribute to `<loomi-modal>`, letting the `icon` attribute pull from
`heroicons` (default), `iconsax`, or `untitledui` — matching `<loomi-icon>`'s own `source`
attribute.
