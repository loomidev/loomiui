---
"@loomidev/icon": minor
---

Add `branded`, `shade`, and `radius` to `<loomi-icon>` for rendering icons on a rounded, primary-colored background badge (`shade="light"` for a soft tint, `shade="dark"` for a solid fill), matching the theme override behavior every other component already has.
