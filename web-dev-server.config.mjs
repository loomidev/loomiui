// Config for local component previews and web-test-runner served fixtures.
// Components are published as native ESM packages, so the default node resolver is enough.
export default {
  nodeResolve: true,
};
