## What does this change?

<!-- Describe what changed and why. Link any related issue with "Fixes #123". -->

## Affected package(s)

<!-- e.g. @loomidev/select, @loomidev/forms -->

## Checklist

- [ ] `pnpm build` passes
- [ ] `pnpm typecheck` passes
- [ ] `pnpm test` passes
- [ ] I added/updated a smoke test if I changed component behavior (see `CONTRIBUTING.md`)
- [ ] I updated the affected package's `README.md` if I changed its public API
- [ ] I ran `pnpm changeset` to record this change (skip for docs-only / internal tooling changes)
- [ ] I manually verified the change in a browser (e.g. via an `examples/*.html` page) if it touches rendering or interaction

## Screenshots / recording (if visual)

<!-- Before/after screenshots or a short clip are very helpful for component changes. -->
